# 文件目录导航

根目录是当前开发与运行区，四份正式notebook继续使用原路径；完整交付包按版本单独保存。目录整理不改变模型和结果。

| 要做什么 | 打开哪里 |
|---|---|
| 阅读/修改当前四问 | `0924_F题_问题一.ipynb`、`0924_问题二.ipynb`、`0925_问题三.ipynb`、`0925_问题四.ipynb` |
| 阅读当前合并正文 | `论文正文.md` |
| 运行全部四问 | 在根目录运行 `Q1/.venv/bin/python run_all.py` |
| 取用完整交付包 | `交付版本/v0.8.0/F题_四问完整复现包/` 或同目录的 `F题_四问完整复现包_v0.8.0.zip` |
| 查看历史草稿与旧结果 | `90_历史方案/`，按问题一、二、四分类 |
| 查看冗余度独立对照 | `Q1_redundancy/`（代码）、`output_q1_redundancy/`（结果） |
| 查找运行日志与临时核查 | `91_临时与日志/` |
| 查找移动前的位置 | `目录整理记录.json`，记录原路径和新路径 |

## 当前工程的固定目录

`notebook_source.py`、`Q2/`、`Q3/`、`Q4/`保存当前源码与辅助程序；`output_q1/length_domain_calibrated22/`、`output_q2_shared/`、`output_q3_resource/`、`output_q4_evolution/`保存当前结果。`real_attachments/`和题目DOCX是输入。`Q1/.venv/`是现有Python环境，保留原位置以避免环境内绝对路径失效；Q1旧工程已归档。

`AGENTS.md`和`PROJECT_MEMORY.md`记录建模约定，`CHANGELOG.md`记录版本。以上当前路径被代码或输入哈希引用，不直接改名。

## 交付与回退

`交付版本/v0.8.0/`是整理前已完成的版本快照，本次只移动，不改内容。`交付版本/重复副本/`保留原“完整复现包 2”：除系统缓存外，其2391个文件与v0.8.0包逐项SHA256一致；为保留原始材料，本次未删除。

以后打包必须指定新的版本号：`Q1/.venv/bin/python build_package.py --version v0.8.1`。脚本以最近的完整包提供附件，复制当前已验证源码与结果；已有版本目录一律拒绝覆盖，失败时不发布半成品。仅执行打包不会自动创建Git标签。

Git保存已跟踪代码、notebook、正文和关键结果；原始附件、完整ZIP、历史实验大文件继续留在本地。历史目录用于查阅，部分旧脚本依赖原目录结构，不能直接当作当前复现入口；如需重跑，按移动清单恢复到独立副本，避免覆盖当前工程。
