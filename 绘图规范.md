# 本项目数模论文绘图规范

本规范用于本项目论文图件的生成和验收，是项目排版约定，不冒充竞赛官方格式要求。核心原则：一张图围绕一个分析问题，图内呈现证据，图号和总图名放在论文的图下注释中。

1. **图内文字。** 删除图片最上方的“图 x ……”整行总标题，不仅删除编号。保留必要的子图说明；多子图按(a)、(b)、(c)连续编号。坐标名称、单位、色标含义和必要统计信息不能随标题一起丢失；移出的说明保存在图注记录中。英文术语统一为 Pearson、Spearman，变量、上下标与正文一致。变量使用LaTeX数学语法渲染，例如`$Q_{\mathrm{lin}}$`、`$Q_{\mathrm{res}}$`、`$\beta$`，图片中不得直接显示代码式下划线；仅修改显示文字，不改数据字段名。
2. **布局与可读性。** 删除子图后重新创建网格并调整画布，禁止用白块覆盖或留下原位置空洞。图例优先使用现有空白区域，不能遮挡曲线、误差带或标签。全宽图在论文中建议宽15—16厘米，缩放后的主要文字以8—10磅为目标；22指标等高密度图应放大或单独占页，不应靠缩小字号强塞进正文。提交排版时以实际打印尺寸再次检查。
3. **视觉与数据一致性。** 白底、深色文字、细浅网格；相同领域跨图保持同色。线型、标记和直接标注辅助区分，不能只依赖红绿配色。保留坐标范围、阈值、数据顺序和统计定义；误差区间需在图注解释。图中关联不表述为因果，候选预测不表述为实测最优。
4. **导出与复现。** 从原始绘图代码重绘，不编辑截图。保留已有PNG与SVG文件名，PNG至少300 dpi，新增PDF用于论文排版，SVG保留文字便于编辑。同步主Notebook与Python镜像；涉及绘图代码修改时从头执行相应Notebook并检查输出。仅展示变化不能改变模型参数和结果表。
5. **验收。** 检查全部导出图无顶部编号总标题、中文缺字、标签截断及图例遮挡。重点核对图1七域图例位于右下空白；图5只有原右侧条形图、横轴明确为Pearson相关系数；图6只有领域比例与长度关联两栏，编号连续。记录输出文件、源代码及结果表一致性；版本通过CHANGELOG、Git提交和标签追踪。

本次图件逻辑：图1比较七域22指标的经验分布；图5展示反向相关最强的15对指标与诊断阈值；图6比较领域冲突比例及其与文本长度的关联。其余图保持原分析问题与数据映射，统一移出编号总标题。所有图属于定量统计图，结论与统计边界沿用已有计算结果。

第四问具体图件逻辑与必须保留的图注边界见`figure_audit/问题四绘图说明.md`。图37与独立流程图共用完整结构，图38—43统一子图编号、数学变量与线型标记；条件区间和描述性对照说明不得省略。
