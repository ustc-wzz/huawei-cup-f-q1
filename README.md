# F题：质量评价、领域配比与标度律

主文档：`0924_F题_问题一.ipynb`。从本目录完整运行，数据目录为 `real_attachments/A_data_value/`；原始附件与缓存不上传，需按本地数据布局另行准备。

## 四问统一复现入口（v0.9.2）

完整包为 `F题_四问完整复现包_v0.9.2.zip`，旧ZIP保持原样。完整解压后Windows双击 `run_windows.bat`；或执行 `python run_all.py`。需已安装Python 3.8或更新版本；当前版本不适合计算依赖时，自动下载包内Python 3.12，不改系统Python；缺依赖时入口自动创建包内独立环境并安装，首次通常需要联网。

运行器自动定位并校验原始附件，缓存缺失/损坏时从A1–A3完整原始数据重建，再顺序运行四问。统一UTF-8并注册随包Noto Sans SC字体。`python run_all.py --prepare-only`只准备环境和缓存；失败后重复运行，或使用`--start-at 2/3/4`续跑。

GitHub仍不上传原始附件和完整ZIP；直接克隆源码不能替代本地完整数据包。面向接收者的简明说明见 `PACKAGE_README.md`，数据清单为 `repro_inputs.json`。当前模型/结果目录及其余历史文件保持原位置。

当前质量冗余度：d_j=1+Σ(k≠j)r_jk²；质量聚合参数β=0.25。四份notebook与源镜像同步，正文通过`python refresh_reports.py`刷新，本地完整包通过`python build_package.py --version v0.9.2`生成独立版本包并校验；已有版本拒绝覆盖。

领域配比与损失建模：比较线性混料、对数线性混料、加性样条和GBM。经综合权衡，选择对数线性模型作为主模型，用于关系解释与约束配比求解；GBM作为对照模型，其预测表现整体较强，用于比较和辅助核查。模型选择不等于单项预测指标排名第一。

## 问题二：共同修正标度律（v0.3.0）

主文档：`0924_问题二.ipynb`；对应文本镜像为 `Q2/notebook_source.py`，根目录的 `notebook_source.py` 仍对应问题一。

采用 `L_s = E_s + (A_s N^(-α) + B_s D^(-ν)) exp(-λu + ηh(p))`，其中 `u = Q-Q0(p)`；来源允许不同基线幅度。质量评分映射未被联合数据标定，接口分别保存已估计的 θ_Q 与未识别的 λ、s_Q。notebook完整展示数据口径、跨来源假设、参数估计与留出验证、边际效用/弹性、领域替代/组合效应、质量—规模替代及适用边界。

在项目根目录运行（需本地原始附件和问题一当前输出）：

```bash
python Q2/export_q1_inputs.py
python Q2/run_shared.py
```

依赖：numpy、pandas、scipy、scikit-learn、matplotlib、nbformat、nbclient、ipykernel。输入位于 `real_attachments/A_data_value/regmix_tables/`、`real_attachments/B_scaling_laws/`；原始附件不在仓库内。第一次命令只补充导出当前问题一结果，不重新选模；第二次从镜像生成固定文件并执行所有单元。

结果位于 `output_q2_shared/`：`results_summary.md` 为结论，`tables/` 为指标与分析，`figures/` 为图，`q2_interface.json` 为后续接口。质量分组留出 RMSE=0.062225，配比分组留出 RMSE=0.094679；完整留尺度误差明显增大，联合质量—配比响应仍是工作假设，不能宣称已获得完全可迁移的实测四因素规律。

## 问题三：算力约束下的资源配置

主文档 `0925_问题三.ipynb`，镜像 `Q3/notebook_source.py`，求解器 `Q3/resource_model.py`。在已有本地输入的项目根目录运行：

```bash
Q1/.venv/bin/python Q3/run_notebook.py
```

新环境依赖及输入要求见 `Q3/README.md` 和 `Q3/requirements.txt`。运行入口实际从头执行所有notebook单元；输出在 `output_q3_resource/`，包括主配置、无提质基线、预算扫描、敏感性、转移表、六组图件、验证JSON和后续接口。

主情景p0、s_Q=1及最高50%文档均值增量参照上限下，10^19—10^24预算内全部达到质量上限，未检测到状态转移。s_Q=2幂函数成本在2048、4096、8192上下文下出现不提质→部分提质→质量上限的转移。结果是共同修正标度律下的条件预测，保留质量正向外推、未识别质量尺度与同一D成本代理的供给局限。

## 问题四：技术演进中介分解与能力前沿（v0.6.1）

主文档`0925_问题四.ipynb`，镜像`Q4/notebook_source.py`；在项目根目录执行`Q1/.venv/bin/python Q4/run_notebook.py`。全部观测仅使用本地附件，完整建模推导、代码运行、验证、七组图件及模型求解位于`output_q4_evolution/`，入口为`results_summary.md`。依赖和数据布局见`Q4/README.md`。

采用时间→N/D→标度响应→能力的带噪声中介模型。35个完整训练元数据基础模型的条件归因约为规模56.98%、非规模43.02%，技术贡献区间包含零；大样本N-only描述性前沿另列，不能混为完整因果归因。C8截断JSON保留原件并按目录选择有效替代。高可比Loss桥接对未来目标损失不支持；预测起点为2025-03-13，12/24个月输出是条件情景，保留结构迁移与外推局限。

## 版本区分

采用语义版本标签 `v主版本.次版本.修订号`：模型或实验流程改变增加次版本；格式和文字修正增加修订号；不兼容的整体框架变更增加主版本。日期、具体修改及验证结果记录在 CHANGELOG.md。

- `v0.8.0`：平方相关冗余赋权、四问结果同步重算、说明精简及重复执行代码清理。
- `v0.1.0`：修改前基线，GBM主预测与候选接口。
- `v0.2.0`：对数线性主模型与解析配比接口，GBM辅助核查。
- `v0.2.1`：新增跨对话项目记忆，模型与实验结果不变。
- `v0.3.0`：问题二共同修正标度律，完整执行及替代关系分析；问题一保持不变。
- `v0.3.1`：统一问题一领域配比模型定位表述，计算方法与数值结果不变。
- `v0.3.2`：问题二同步相同口径，完整重跑，结果不变。
- `v0.3.3`：问题一首页与文本镜像同步，结果不变。
- `v0.3.4`：核对已提交的问题二，补齐项目记忆、单位和接口说明，计算方案不变。
- `v0.3.5`—`v0.3.6`：第三问质量接口核查、七项选择确认与执行规范。
- `v0.4.0`：第三问正式notebook完整执行，交付配置、转移分析、敏感性及数值核验。
- `v0.5.0`：第三问五档质量上限、全局收益阈值与资源权衡分析。
- `v0.6.0`：第四问结构中介、JSON逐任务审计、分层桥接、前沿预测及完整方法／求解章节；前三问计算文件不变。
- `v0.6.1`：修复第四问块公式渲染与KaTeX控制字符错误，完整重跑，数值结果不变。

## 新对话的项目记忆

在本项目目录中新建Codex任务时，`AGENTS.md`提供精简项目指令；需要公式和数据关系时，按其指引读取`PROJECT_MEMORY.md`。无需每次粘贴历史聊天。若使用另一个工作副本，需先确保该副本包含这两个文件。已有对话可明确要求重新读取。

可直接发送：

> 请按本项目AGENTS.md继续工作，需要模型细节时按需读取PROJECT_MEMORY.md。沿用已确认且当前代码采用的方案；区分已实现结论与待验证建议。完成本次工作后，把新确认的决定和验证结果更新到项目记忆，替换过时内容，不追加聊天流水账。

每次修订完成后完整验证、提交并创建带说明的版本标签，然后同步分支和标签到GitHub。文件名保持稳定，版本由Git管理；`notebook_source.py`是包含Markdown的文本镜像，便于查看改动。

查看版本：`git log --oneline --decorate`；查看两个版本的源代码变化：`git diff v0.1.0 v0.2.0 -- 0924_F题_问题一.ipynb`。GitHub可通过Tags切换版本，通过Compare比较两版。
