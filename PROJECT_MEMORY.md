# 项目记忆：当前采用的方法与证据边界

核对日期：2026-09-25。问题一模型基线：v0.2.0（9cb3d1b）。本文件按需阅读，自动加载入口为同目录 AGENTS.md。后续用户决定及经核验的新实现优先；这里只记录已采用的方法，不保存完整聊天。

## 1. 文件入口

| 用途 | 项目相对路径 |
|---|---|
| 问题一主文档 | `0924_F题_问题一.ipynb` |
| 可搜索文本镜像 | `notebook_source.py` |
| 当前结果目录 | `output_q1/length_domain_calibrated22/` |
| 问题二/三接口 | `output_q1/length_domain_calibrated22/tables/q1_outputs_for_q2_q3.json` |
| 配比模型指标 | `output_q1/length_domain_calibrated22/tables/mixture_model_metrics_long.csv` |
| 数据来源与加工性质 | `real_attachments/source_manifest.json` |
| 版本历史 | `CHANGELOG.md`、Git提交与标签 |

同目录其他问题一notebook和阅读指南可能属于历史方案；除非指定比较，不用它们覆盖主文档。原始附件和大部分缓存不在Git仓库，换机器后需另行准备。

## 2. 质量评分与冲突：三种量分别解释

22个指标直接评价每篇文本，不再经过四维分组。A1有51,230条，A2为17,523条arxiv扩展，A3为203,752条github扩展；这些是当前文件基线数量，换数据需重核。七个质量域为 arxiv、book、c4、commoncrawl、github、stackexchange、wikipedia；A3不是第八个领域。

预处理主线：统一指标方向 → 独特词占比按A1长度分位分20组校正 → 行末标点占比与非字母词占比按A1同领域参考校准 → 正态分位分数Z。专业性保持全局A1映射；映射、权重与冲突尾部阈值固定应用于A2/A3。校准是改变比较参照，不能仅凭冲突率下降证明准确性提高。

相关性去冗余赋权＝降低与其他指标共享信息较多的指标权重。有效指标上 d_j=1+Σ(k≠j)|r_jk|，w_j=d_j^(-α)/Σd_l^(-α)，α=1；常数指标权重为0。低相关不等于更可靠。

线性基线 Q_lin=Σw_jZ_j；最终质量分 Q=−ln(Σw_j exp(−βZ_j))/β，β=0.25，对应代码列 `Q_res`。它强调短板并限制高分补偿低分，可为负。β减小则趋近线性基线；β=0.25是用户确认的温和惩罚设置，不是经独立标签证明的最优值。book的长文本句子数惩罚仍需谨慎解释，排名不能直接当作客观质量事实。

冲突诊断独立计算：A1正态分数Pearson r≤−0.30选候选对；同一文本在该对上严格超过80%分位、严格低于20%分位才触发，CI按负相关强度及分数差加权，CI>0为冲突信号。主阈值0.30，0.20/0.40/0.50为敏感性分析。分清“冲突指标对占比”“单对冲突率”“冲突样本占比”及各自分母；不可判定不能算成无冲突。β影响Q不影响诊断；只改诊断阈值不影响Q。Q也会下调没有触发负相关冲突的短板样本，不能称其消除了统计冲突。

领域汇总有文档均值与词数加权均值两种口径，引用排名必须说明。后续域映射用文档均值，arxiv/github用抽样与扩展合并去重口径。配方平均质量是Σp_jQ_j，不是训练效果。

## 3. 领域配比：主模型定位与数据关系

领域配比与损失建模：比较线性混料、对数线性混料、加性样条和GBM。经综合权衡，选择对数线性模型作为主模型，用于关系解释与约束配比求解；GBM作为对照模型，其预测表现整体较强，用于比较和辅助核查。模型选择不等于单项预测指标排名第一。

RegMix的512组训练记录＝同一约1M参数规模、不同配方的512次训练，不是512种模型规模。`real_attachments/A_data_value/regmix_tables/train_mixture_1m.csv` 为17域配比，`train_pile_loss_1m.csv` 为13域验证损失，以 `index` 关联。目标是13域损失均值。测试配方有1M/256组、60M/256组、1B/64组。

17域字段为 arxiv、freelaw、nih_exporter、pubmed_central、wikipedia_en、dm_mathematics、github、philpapers、stackexchange、enron_emails、gutenberg_pg_19、pile_cc、ubuntu_irc、europarl、hackernews、pubmed_abstracts、uspto_backgrounds。七域与十七域来自不同分类体系，映射与缺失域均值填补是近似，不是实测同质量。

主模型：L_hat(p)=a0+Σa_j ln(p_j+ε)，当前ε=0.001；p_j≥0，Σp_j=1。以KKT约束最优条件求候选配比、SLSQP数值核验，GBM辅助评价。解释应指定从哪个域向哪个域转移份额；含ε时 a_j 不等于严格的对数份额导数，系数正负不能单独证明领域好坏或因果互补性。

当前四模型为线性、对数线性、PDF加性样条、GBM；训练集内部调参，嵌套五折评价，检验集不参与选参。v0.2.0中对数线性交叉验证R²=0.8450，GBM=0.9105；1B排序相关分别0.7548、0.7211。这支持“解释与求解的权衡”，不支持“对数线性预测全面领先”。跨尺度绝对损失误差仍大；推荐配比未经新增真实训练验证。

依赖关系：22项评分→Q→域质量汇总；17域配比→真实平均损失→代理模型拟合与配比求解。当前第二条不输入Q，所以换质量聚合方法或β不会自动改变配比模型。当前Q没有跨尺度一致的额外预测增益；问题二若连接两条链，须明确新增假设、尺度校准和是否重复计入质量。

## 4. 问题二：共同修正标度律（v0.3.0）

主文件 `0924_问题二.ipynb`，镜像 `Q2/notebook_source.py`，结果 `output_q2_shared/`。采用 L_s=E_s+(A_sN^-α+B_sD^-ν)exp[-λu+ηh(p)]，u=Q-Q0(p)；h由问题一对数线性预测以训练参考标准化。来源共享指数、允许基线幅度不同，来源校准不等于未知来源直接迁移。

实际运行：B1的α=0.339985、ν=0.279892；质量θ_Q=0.396659，分组留出RMSE=0.062225（无质量项0.207307）；配比η=0.079567，分组留出RMSE=0.094679（无配比项0.237195）。留尺度1B的RMSE=0.689102，跨尺度外推明显受限。质量q_B和问题一Q并非同一评分，只识别θ_Q=λs_Q；s_Q=0.5、1、2为情景，不是实测标定。B7质量数据是半合成，B8方向冲突另报敏感性；独立的A、B不能验证质量与配比的联合可分离性。

已实现边际效用、N/D弹性及Q半弹性，固定u/固定Q配比导数的区别、领域可行转移/共同补偿域组合效应、质量—规模双向等损失条件、条件训练算力最优分配；解析关系通过数值核验。领域组合效应是非线性和配比约束下的条件预测，不是直接估计的因果交互。实际成本仍用原始N、D，Q可负，不对Q取对数。新增结果以实际产物核对，不沿用旧有效数据量模型数值。

## 5. 保持精简与更新

已替换的方法只保留这一行边界，不展开推导或主动重做：四维分组、熵权/几何平均、β=1主设定、对数二阶加岭回归候选、GBM主模型均非当前主方案；样条仍是有效对照，不能误列为全部弃用。

新结论只有在用户确认，或完成实现和验证后，才替换对应条目；待验证建议明确标注待定。数值变化附版本，不无限追加历史。保持AGENTS.md短，只在确有需要时读取本文件。

## 6. 提炼来源（追溯时才读）

选取同一工作目录下、本轮开始时最近六个非活跃窗口，按最近更新时间排序；通过read_thread分页至hasMore=false，共78轮。读取的是工具可提供的用户消息、回答及操作记录；图片原件和全部工具输出未逐项重放，最终模型另与当前代码、接口、README及CHANGELOG交叉核对。

| 对话原名 | 轮数 | 对话ID | 保留内容 |
|---|---:|---|---|
| 0924问题一（正态） | 24 | 01a0d23f-1eb4-7cd2-a80b-741ff8591c01 | β、最终主模型、验证口径、Git版本约定 |
| 解释问题二解题思路 | 5 | 01a0d30f-610d-7463-97db-1dfb8d62d41c | 符号解释、分层验证及证据边界；旧公式不作定论 |
| 数据文件 | 21 | 01a0cdbe-c743-72e2-8784-7cb028242745 | 七域/十七域、配比/结果表关联、轨迹性质 |
| 独特词/专业性 | 25 | 01a0d1b3-1647-76a1-a9b2-284e0ebe504a | 取消四维、去冗余赋权、长度与格式校准 |
| 检验参考问题一的冲突率建模合理性 | 1 | 01a0d21a-b8eb-7911-8078-d62dfc8bbdaa | 分母、阈值和不可用降低冲突率证明有效 |
| 解释 SlimPajama-Meta-rater 数据集 | 2 | 01a0d19f-ad0b-7250-836d-3ecb47c23551 | 已有模型评分与本项目计算的来源区别 |

筛选规则：最终用户决定与实现一致才固化；早期回答中四维分组、旧主模型、旧质量尺度和字段拼写错误已由当前证据纠正。未保存账号授权流程、临时故障、原始聊天或弃用方法细节。
