# 项目记忆：当前采用的方法与证据边界

核对日期：2026-09-26；当前计算与交付基线为整包v0.9.2，记忆修订v0.9.3（仅文档，不生成新ZIP）。问题一冗余度采用平方相关，四问接口与结果已重算；其余模型结构保持原方案。本文只记录已采用的方法，具体数值以当前结果为准。

四问承接主线：问题一输出平方相关质量评分与对数线性配比接口 → 问题二估计质量/配比共同修正标度律 → 问题三在给定预算下求N、D、额外质量u → 问题四借助标度响应分解历史能力变化，并将问题三最优资源路径用于条件能力前沿预测。问题一的质量评分与配比回归原本独立；后续联合使用是明确的建模连接，不等于观测数据已验证联合因果关系。

## 1. 文件入口

v0.9.14论文绘图：新增`绘图规范.md`；figures中图1—16删除顶部编号总标题，图1七域图例移到右下空白，图5删除左图并明确Pearson横轴，图6删除中间指标对子图并改为(a)/(b)两栏。第一问主Notebook及镜像同步，从头执行23个代码单元；20组PNG/SVG/PDF已导出，89个结果表经数值等价核验后保留原字节与接口哈希。模型、其他问及复现ZIP不变。核验记录在`figure_audit/`。

论文表达副本（v0.9.12，问题一）：`F题论文初稿_问题一5.8至5.10精简_20260926.docx`，基于用户附件`F题论文初稿_格式修复_202609261400(1).docx`仅修改5.8—5.10并新增附录一；正文按质量关联→主模型候选→验证组织，减少38.3%，保留6个核心公式，7个补充公式及两张复现表移入附录。主配方为当前对数模型KKT解，名次融合仅作辅助对照；更正文稿中的随机候选生成说明以匹配当前代码。脚本`Q1/simplify_paper_sections.py`，正文、附录快照和核验记录在`Q1/`。其他章节及原稿保留，未合并其他论文副本，数值模型、Notebook与ZIP不变。

| 用途 | 项目相对路径 |
|---|---|
| 问题一主文档 | `0924_F题_问题一.ipynb` |
| 可搜索文本镜像 | `notebook_source.py` |
| 当前结果目录 | `output_q1/length_domain_calibrated22/` |
| 问题二/三接口 | `output_q1/length_domain_calibrated22/tables/q1_outputs_for_q2_q3.json` |
| 配比模型指标 | `output_q1/length_domain_calibrated22/tables/mixture_model_metrics_long.csv` |
| 问题二主文档与镜像 | `0924_问题二.ipynb`、`Q2/notebook_source.py` |
| 问题一向问题二补充导出 | `output_q1/length_domain_calibrated22/q2_loglinear_inputs/` |
| 问题二结论与后续接口 | `output_q2_shared/results_summary.md`、`output_q2_shared/q2_interface.json` |
| 问题二检验结果 | `output_q2_shared/tables/quality_validation.csv`、`output_q2_shared/tables/mixture_validation.csv` |
| 问题三主文档与镜像 | `0925_问题三.ipynb`、`Q3/notebook_source.py` |
| 问题三求解与全局阈值 | `Q3/resource_model.py`、`Q3/decision_analysis.py` |
| 问题三结果 | `output_q3_resource/results_summary.md`、`output_q3_resource/decision_summary.md` |
| 问题四主文档与镜像 | `0925_问题四.ipynb`、`Q4/notebook_source.py` |
| 问题四模型与结果 | `Q4/evolution_model.py`、`output_q4_evolution/模型求解.md` |
| 四问完整公式来源 | `Q1/模型建立.md`、`Q2/模型建立.md`、`Q3/模型建立.md`、`Q4/模型建立.md` |
| 数据来源与加工性质 | `real_attachments/source_manifest.json` |
| 版本历史 | `CHANGELOG.md`、Git提交与标签 |

同目录其他问题一notebook和阅读指南可能属于历史方案；除非指定比较，不用它们覆盖主文档。Git仓库不含原始附件；完整ZIP包含必要原始附件，可自动定位并重建缓存，接收ZIP的用户无需手工准备signals_cache。

## 2. 质量评分与冲突：三种量分别解释

22个指标直接评价每篇文本，不再经过四维分组。A1有51,230条，A2为17,523条arxiv扩展，A3为203,752条github扩展；这些是当前文件基线数量，换数据需重核。七个质量域为 arxiv、book、c4、commoncrawl、github、stackexchange、wikipedia；A3不是第八个领域。

预处理主线：统一指标方向 → 独特词占比按A1长度分位分20组校正 → 行末标点占比与非字母词占比按A1同领域参考校准 → 正态分位分数Z。专业性保持全局A1映射；映射、权重与冲突尾部阈值固定应用于A2/A3。校准是改变比较参照，不能仅凭冲突率下降证明准确性提高。

相关性去冗余赋权＝降低与其他指标共享信息较多的指标权重。有效指标上 d_j=1+Σ(k≠j)r_jk²，w_j=d_j^(-α_w)/Σd_l^(-α_w)，α_w=1（赋权指数，与标度律α区分）；代码有效阈值为标准差>1e-12，其余指标权重为0。旧绝对相关|r_jk|已被平方r_jk²替换，不能恢复为当前方案。低相关不等于更可靠。

线性基线 Q_lin=Σw_jZ_j；最终质量分 Q=−ln(Σw_j exp(−βZ_j))/β，β=0.25，对应代码列 `Q_res`。它强调短板并限制高分补偿低分，可为负。β减小则趋近线性基线；β=0.25是用户确认的温和惩罚设置，不是经独立标签证明的最优值。book的长文本句子数惩罚仍需谨慎解释，排名不能直接当作客观质量事实。

冲突诊断独立计算：A1正态分数Pearson r≤−0.30选候选对；同一文本在该对上严格超过80%分位、严格低于20%分位才触发，CI按负相关强度及分数差加权，CI>0为冲突信号。候选集合E={(j,k):j<k,r_jk≤−0.30}，高低指示量采用严格不等式；候选集合为空或权重和为零时CI=0。主阈值0.30，0.20/0.40/0.50为敏感性分析。分清“冲突指标对占比”“单对冲突率”“冲突样本占比”及各自分母；不可判定不能算成无冲突。β影响Q不影响诊断；只改诊断阈值不影响Q。Q也会下调没有触发负相关冲突的短板样本，不能称其消除了统计冲突。

领域汇总有文档均值与词数加权均值两种口径，引用排名必须说明。后续域映射用文档均值，arxiv/github用抽样与扩展合并去重口径。配方平均质量是Σp_jQ_j，不是训练效果。

- 问题一冗余度独立对照（v0.7.2）：固定预处理与β=0.25，平方相关在44个副本实验均减小排名扰动；200次分层重抽样改善较小且非所有指标占优。42对盲评待人工标注，不能认定质量准确性提高；v0.8.0已将平方相关用于主模型并重算后续接口；盲评状态仍为待完成。入口 `output_q1_redundancy/实验报告.md`，脚本 `Q1_redundancy/`。

## 3. 领域配比：主模型定位与数据关系

领域配比与损失建模：比较线性混料、对数线性混料、加性样条和GBM。经综合权衡，选择对数线性模型作为主模型，用于关系解释与约束配比求解；GBM作为对照模型，其预测表现整体较强，用于比较和辅助核查。模型选择不等于单项预测指标排名第一。

RegMix的512组训练记录＝同一约1M参数规模、不同配方的512次训练，不是512种模型规模。`real_attachments/A_data_value/regmix_tables/train_mixture_1m.csv` 为17域配比，`train_pile_loss_1m.csv` 为13域验证损失，以 `index` 关联。目标是13域损失均值。测试配方有1M/256组、60M/256组、1B/64组。

17域字段为 arxiv、freelaw、nih_exporter、pubmed_central、wikipedia_en、dm_mathematics、github、philpapers、stackexchange、enron_emails、gutenberg_pg_19、pile_cc、ubuntu_irc、europarl、hackernews、pubmed_abstracts、uspto_backgrounds。七域与十七域来自不同分类体系，映射与缺失域均值填补是近似，不是实测同质量。

主模型：L_hat(p)=a0+Σa_j ln(p_j+ε)，当前ε=0.001；p_j≥0，Σp_j=1。以KKT约束最优条件求候选配比、SLSQP数值核验，GBM辅助评价。解释应指定从哪个域向哪个域转移份额；含ε时 a_j 不等于严格的对数份额导数，系数正负不能单独证明领域好坏或因果互补性。

当前四模型为线性、对数线性、PDF加性样条、GBM；只在512条1M训练集内做外层五折、内层五折，检验集不参与选参；不能写成跨尺度分组交叉验证。历史对照（v0.2.0，引用当前精确值须核对结果表）：对数线性交叉验证R²=0.8450，GBM=0.9105；1B排序相关分别0.7548、0.7211。这支持“解释与求解的权衡”，不支持“对数线性预测全面领先”。跨尺度绝对损失误差仍大；推荐配比未经新增真实训练验证。

依赖关系：22项评分→Q→域质量汇总；17域配比→真实平均损失→代理模型拟合与配比求解。当前第二条不输入Q，所以换质量聚合方法或β不会自动改变配比模型。当前Q没有跨尺度一致的额外预测增益；问题二若连接两条链，须明确新增假设、尺度校准和是否重复计入质量。

## 4. 问题二：共同修正标度律（当前实现）

论文表达副本（v0.9.11）：`F题论文初稿_问题二图文优化_20260926.docx`，在保留的v0.9.10精简副本上新增三张核心结果图，按修正有效性→质量参数替代→配比最终损失响应组织，正文保留16个核心公式、2张结果表。跨尺度误差并入图6-1；规模验证与原17×17热图置于附录。质量替代固定7B参数参照、140B Token，刻度0.5/1/2均为条件情景；配比图固定u=0，展示统一模型损失而非第一问预测量。脚本为`Q2/draw_core_figures.py`和`Q2/insert_core_figures.py`，图件、CSV及核验在`output_q2_paper/`。其他章节保留，未合入v0.9.9第四问副本；原参数、Notebook、计算结果和复现ZIP未变。

主文件 `0924_问题二.ipynb`，镜像 `Q2/notebook_source.py`，结果 `output_q2_shared/`。采用 L_s=E_s+(A_sN^-α+B_sD^-ν)exp[-λu+ηh(p)]，u=Q-Q0(p)；h由问题一对数线性预测以训练参考标准化。来源共享指数、允许基线幅度不同，来源校准不等于未知来源直接迁移。

Q0(p)=Σp_jQ_j；h(p)=[f(p)−f(p0)]/s_L，f是问题一对数线性预测，p0为训练集平均配比，s_L为训练真实平均损失的标准差。u表示相同配比下额外质量变化；固定u和固定Q的配比变化含义不同。N、D都以十亿计，实际算力C=6×10^18ND FLOPs，不可直接把数值代入无单位换算的6ND。质量与配比共同乘子作用于参数项和数据项之和，不是只修正数据项的有效数据量方案。

实际运行：B1的α=0.339985、ν=0.279892；质量θ_Q=0.396659，分组留出RMSE=0.062225（无质量项0.207307）；配比η=0.079567，分组留出RMSE=0.094679（无配比项0.237195）。留尺度1B的RMSE=0.689102，跨尺度外推明显受限。质量q_B和问题一Q并非同一评分，只识别θ_Q=λs_Q；s_Q=0.5、1、2为情景，不是实测标定。B7质量数据是半合成，B8方向冲突另报敏感性；独立的A、B不能验证质量与配比的联合可分离性。

估计目标分阶段：B1采用对数残差Huber损失；来源幅度校准、质量θ_Q及配比η采用各自的约束最小二乘，不能统称全部Huber拟合。

已实现边际效用、N/D弹性及Q半弹性，固定u/固定Q配比导数的区别、领域可行转移/共同补偿域组合效应、质量—规模双向等损失条件、条件训练算力最优分配；解析关系通过数值核验。领域组合效应是非线性和配比约束下的条件预测，不是直接估计的因果交互。实际成本仍用原始N、D，Q可负，不对Q取对数。新增结果以实际产物核对，不沿用旧有效数据量模型数值。

固定质量、配比及来源时，共同乘子降低或提高损失水平，但不改变给定训练算力下的最优N/D分配；质量获取成本或质量随N/D变化时不能直接沿用此结论。接口的 `lambda`、`s_Q` 为null，不能将情景s_Q=1下的λ=θ_Q当作独立估计结果。

复现链：先在项目根目录运行 `python Q2/export_q1_inputs.py` 补充导出问题一当前接口，再运行 `python Q2/run_shared.py` 生成并执行问题二。问题二读取附件B与补充导出，不直接读取附件A；接口哈希用于检查承接一致性。问题一检验集已被查看，问题二RegMix检验属于事后迁移诊断，不能称全新盲测；同配方跨尺度记录按组留出。原CSV不含逐运行D，当前1M/60M的1B token及1B模型的25B token来自文献元数据匹配，是分析条件。

## 5. 问题三：条件资源优化（当前实现）

问题三方向：用户已舍弃方案二，后续按方案一的条件资源优化推进。2026-09-25完成质量—成本接口数据核查，脚本 `Q3/audit_quality_interface.py`，报告 `Q3/quality_audit/核查与待决策.md`。272,505条评分合并为261,086篇，域均值复现接口；p0/p*中无质量样本的11域份额为43.54%/54.66%。用户随后已确认七项推荐：p0主/p*对照；A1七域等权1%—99%线性成本映射；最高80%/50%/20%/10%/5%文档均值增量形成并列上限情景，50%仅是展示切片；11缺失域零新增收益且冻结Q_bar；s_Q=1主、0.5/2敏感性；允许显式规模外推；题设同一D成本代理。完整执行规范见 `Q3/第三问notebook执行Prompt.md`。质量上限是样本参照的情景上限，状态R2定义u=u_max而非z=1；数值搜索边界不能当物理约束。第三问正式notebook已实现并完整执行。

主文档 `0925_问题三.ipynb`；镜像 `Q3/notebook_source.py`；求解器 `Q3/resource_model.py`；运行 `Q1/.venv/bin/python Q3/run_notebook.py`；结果和接口在 `output_q3_resource/`。实际使用Python 3.12.14，依赖版本在 `Q3/requirements.txt` 和结果versions.json。原始附件与评分缓存不上传Git；完整ZIP提供原始数据和自动缓存流程。这里的3.12.14是既有实验环境记录，不是启动Python的唯一允许版本。

B1基线共同修正损失、固定p下优化N/D/u；C=D(kN+c(u))，N/D以十亿计，k=10^18(6+2e-4L_ctx)，c=10^9增量g。内层在log N上严格凸，以自适应括区间KKT求根，外层比较端点和检测到的全部内点候选，并网格加密。无真实硬件上下界，不把观测范围当成物理约束。

问题三承接平方相关评分后重新生成成本锚点、五档上限、最优N/D/u、转移预算及全局收益阈值。当前数值与状态见 `output_q3_resource/results_summary.md`、`decision_summary.md` 和 `tables/transitions.csv`；不沿用v0.5.0绝对相关评分下的常数。

数值核查包含预算残差、KKT、有限差分、网格加密、独立二维优化、转移解析候选及1125次全局阈值决策核验。上限依旧只是文档筛选参照；book最严格尾部9篇，训练Token供给和质量尺度未识别。

主情景s_Q=1的225个配置均为R2（达到样本参照上限）；50%切片在10^19—10^24 FLOPs、五个窗口和三种成本下未出现状态转移。s_Q=2的幂函数成本在2048/4096/8192窗口各出现R0→R1、R1→R2。不能把主情景画成必然三阶段。全局λ_on/λ_full由内层重优化后的H_C割线极值确定，端点导数只作局部诊断；阈值相等允许直接R0→R2。

## 6. 问题四：结构中介与技术演进（当前实现）

显示规范：v0.6.1将章节块公式统一为`$$`，修复一处`\beta`退格字符，源章节、生成器和notebook同步；运行入口检查Markdown控制字符与块分隔符。计算结果不变。

用户确认：可比规模和评测条件下，剩余时间效应代表非规模技术进步；全部观测使用自己的附件，不加入外部数据。主文件`0925_问题四.ipynb`，文本镜像`Q4/notebook_source.py`，入口`Q4/run_notebook.py`，核心模块`Q4/data_pipeline.py`和`Q4/evolution_model.py`。结果、完整模型建立／求解章节、图表及核验在`output_q4_evolution/`。

模型为M=(lnN,lnD)=a0+aT*T+εM；x=-ln(A*N^-α+B*D^-ν)承接问题二；logit(S/100)=β0+κx+γT+εY。噪声从实际残差估计，保留N/D残差相关，跨方程独立是识别假设；四反事实角点的对称分解保证能力分贡献严格可加。机构整组bootstrap 400次，并做自由N/D结构、遗漏混杂与D尺度敏感性。不能声称噪声或bootstrap解决了未观测混杂。

C1筛选去重后2449个模型；确定性C4匹配69个；历史完整ND主样本35个pretrained、10家机构。主样本用发布日期，大样本用提交日期，不混用。2023-04-03→2024-06-07反事实对比，规模2.939分（56.98%）、技术2.219分（43.02%），技术贡献95%区间[-1.754,5.887]分，不能声称精确确定技术份额。自由N/D对照规模份额44.44%，需保留结构敏感性。

C8共1958个JSON，4个损坏：1个目录使用可解析替代，3个目录无可用记录。保留全部原件、哈希与错误台账；最终1860个可解析模型、1856个MuSR三任务完整值，不代表六大任务都完整。三叶子任务各扣除随机猜测基线再等权；最新评测与C1快照差异保留，不覆盖原榜单。

预测起点2025-03-13，未来12/24个月是2026-03-13/2027-03-13，不是从运行日开始。主Q3耦合场景沿用p0、50%参考切片、s_Q=1、4096上下文、指数成本；算力年对数增速取历史估计的1/4，技术趋势半衰期24个月，另做冻结/半增速及半衰期敏感性。基础模型名义90%条件前沿约45.83/51.30分，属于显式规模／时间外推。结构分位模型机构留出覆盖82.86%、发布队列覆盖70.37%，不足名义90%，不能当作有90%保证的未来上界。

C6高／中可比层分别等渗桥接，支撑范围之外返回缺失；高可比层无法换算主未来Loss，中可比只作跨报告情景。不能用结构预测掩盖桥接证据不足。大样本N-only参考分布90%前沿仅作描述性：对话／微调1—3个月回测MAE约1.419，优于上月延续1.990；基础和合并未胜出。元数据事后快照、D缺失选择、未识别质量尺度与长期技术假设仍保留。24项核验仅检查数值恒等式、预算、支撑与输入不变性，不证明因果。

## 7. 保持精简与更新

已替换的方法只保留这一行边界，不展开推导或主动重做：绝对相关冗余度|r_jk|、四维分组、熵权/几何平均、β=1主设定、对数二阶加岭回归候选、GBM主模型均非当前主方案；样条仍是有效对照，不能误列为全部弃用。

新结论只有在用户确认，或完成实现和验证后，才替换对应条目；待验证建议明确标注待定。数值变化附版本，不无限追加历史。保持AGENTS.md短，只在确有需要时读取本文件。

## 8. 提炼来源（追溯时才读）

当前核对以四问代码、四个模型建立文件、问题二结果摘要、问题三决策摘要、问题四模型求解及formula_verification.json为准；以下对话只用于追溯早期决定，不限定当前记忆覆盖范围。

初次提炼选取同一工作目录下当时最近六个非活跃窗口，按最近更新时间排序；通过read_thread分页至hasMore=false，共78轮。读取的是工具可提供的用户消息、回答及操作记录；图片原件和全部工具输出未逐项重放，最终模型另与当前代码、接口、README及CHANGELOG交叉核对。

问题二补充依据：用户确认最新版本已提交后，核对v0.3.0—v0.3.3提交、`Q2/notebook_source.py`、结果摘要、验证CSV及两问接口哈希。对应任务为“构造问题二标度律公式”（01a0d358-7768-7952-93d1-a358a262cd80）；本次工具返回最新轮次正文为空，故采用已提交产物确认实现，不声称读完该窗口全部讨论。

| 对话原名 | 轮数 | 对话ID | 保留内容 |
|---|---:|---|---|
| 0924问题一（正态） | 24 | 01a0d23f-1eb4-7cd2-a80b-741ff8591c01 | β、最终主模型、验证口径、Git版本约定 |
| 解释问题二解题思路 | 5 | 01a0d30f-610d-7463-97db-1dfb8d62d41c | 符号解释、分层验证及证据边界；旧公式不作定论 |
| 数据文件 | 21 | 01a0cdbe-c743-72e2-8784-7cb028242745 | 七域/十七域、配比/结果表关联、轨迹性质 |
| 独特词/专业性 | 25 | 01a0d1b3-1647-76a1-a9b2-284e0ebe504a | 取消四维、去冗余赋权、长度与格式校准 |
| 检验参考问题一的冲突率建模合理性 | 1 | 01a0d21a-b8eb-7911-8078-d62dfc8bbdaa | 分母、阈值和不可用降低冲突率证明有效 |
| 解释 SlimPajama-Meta-rater 数据集 | 2 | 01a0d19f-ad0b-7250-836d-3ecb47c23551 | 已有模型评分与本项目计算的来源区别 |

筛选规则：最终用户决定与实现一致才固化；早期回答中四维分组、旧主模型、旧质量尺度和字段拼写错误已由当前证据纠正。未保存账号授权流程、临时故障、原始聊天或弃用方法细节。

## 复现包自动准备（v0.9.2）

最新本地交付为`F题_四问完整复现包_v0.9.2.zip`，旧包保持原样。入口`run_all.py`允许Python3.8+启动；非64位3.12/3.13通过`python_bootstrap.py`自动准备包内3.12运行环境，依赖安装到`.repro-env`。uv从PyPI下载并校验固定SHA256，Python位于`.repro-python`，不修改系统安装或PATH；`repro_runtime.py`定位并校验原始附件、注册随包中文字体，`signal_cache.py`校验或从完整原始数据重建缓存。四问显式UTF-8，Windows使用`run_windows.bat`；重复运行复用环境和完整缓存，更新当前输出。原始数据与独立环境不上传GitHub。模型沿用v0.8.0；Windows兼容性静态检查不能写成Windows实机运行验证。

## 模型说明完整性（v0.9.2）

正式方法来源为`Q1/模型建立.md`、`Q2/模型建立.md`、`Q3/模型建立.md`和`Q4/模型建立.md`。`refresh_reports.py`按来源刷新合并正文，Q3/Q4运行时刷新方法产物；修改公式须同步对应notebook说明与源镜像。保留指标构造、三类参照、边界情形、估计目标、约束求解及不确定性定义，不能以突出核心公式为由省去必要符号。问题一为1M训练集内嵌套五折，问题二配比迁移诊断才按完整配方跨尺度分组；两者不可混写。计算模型和参数不因本次说明补全而改变。

本次记忆整理未改动模型、notebook、数据或结果；v0.9.2已有四问完整重跑、161张CSV结果表不变及1120处公式渲染检查记录，见`formula_verification.json`。记忆更新不代表新增实机验证。

- v0.9.8独立论文流程图采用浅蓝、浅绿、浅橙三类圆角节点，文字仅置于节点内：`figures/q4_causal_publication.png`（600 dpi）及PDF/SVG；复现入口`Q4/draw_causal_publication.py`。显式展示Z节点及T→Z路径；本次未替换既有Notebook图或重建ZIP，数值模型不变。

- v0.9.9论文副本 `F题论文初稿_问题四重写_20260926.docx` 仅重写第四问，按历史归因→桥接检验→条件前沿→分类型对照组织，补入实际结果与验证边界。复现脚本 `rewrite_q4_paper.py`，正文快照及核验记录位于Q4；原稿与前三问保留，数值模型、Notebook及ZIP不变。

- v0.9.13第三问论文副本 `F题论文初稿_问题三精简_20260926.docx` 基于用户原稿仅重写第三问：预算模型→降维求解→主情景配置与全局阈值→资源收益；正文5页、13个核心公式，辅助推导与设置移至6页附录三。脚本 `rewrite_q3_paper.py`，快照与核验位于Q3；保留主情景无转移及条件边界，未合入其他问的独立副本，数值模型、Notebook及ZIP不变。
