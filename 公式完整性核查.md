# v0.9.2 公式完整性与实现一致性核查

本次补全针对当前四问实现，不另行改变模型、参数、数据或优化目标。正式方法来源为 `Q1/模型建立.md`、`Q2/模型建立.md`、`Q3/模型建立.md`、`Q4/模型建立.md`；合并正文由 `refresh_reports.py`读取这些来源。第一、二问相应notebook说明同步展开，第三问显示完整方法章节，第四问沿用动态载入完整方法章节。

| 环节 | 补全或纠正 | 实现依据 |
|---|---|---|
| Q1指标构造与预处理 | 列表压缩、DSIR每词化、缺失填补、区间效用、Min–Max常数列、三类参考集合、共同截断量和最终Z | `signal_cache.py:compress_record`；`notebook_source.py:build_indicators/interval_score/normalized_utility/length_percentile/calibrated_scores` |
| Q1赋权与聚合 | 有效指标阈值、Pearson完整定义、平方冗余、常数权重、β=0极限、边际单调性及域均分口径 | `notebook_source.py:fit_redundancy`及评分单元 |
| Q1冲突 | E、c、A1分位界、H/L、触发量、分母、空集合取零、冲突率 | `notebook_source.py:sample_conflict`；阈值不进入Q |
| Q1配比 | 四模型形式、训练目标、嵌套五折范围、KKT乘子与互补条件、两种约束替代方向、质量映射收缩 | `notebook_source.py:model_spec/fit_train_only/p_closed`及域替代与QMAP单元 |
| Q2估计 | B1对数Huber、B2非负幅度、B4/B5来源内留一、B7质量最小二乘、配比最小二乘和参数边界 | `Q2/notebook_source.py:fit_classic/quality_fit/mix_fit`；纠正全部阶段都用Huber的概括 |
| Q2解析关系 | λ与s_Q不可分别识别、边际/弹性、等损失可达条件、固定质量训练最优、固定u与固定Q的区别 | `Q2/notebook_source.py:predict_loss/equivalent_ratio`及有限差分检验 |
| Q3资源优化 | 平衡分位插值、筛选取整与上限、三类成本与导数、单位换算、预算消元、严格凸内层、外层导数和全局候选 | `Q3/audit_quality_interface.py:balanced_quantile`；`Q3/resource_model.py:g/gp/inner/solve` |
| Q3决策阈值 | R0/R1/R2容差、全局割线阈值及等号、影子价格、等效预算；退化U=0不伪称当前求解器支持 | `Q3/decision_analysis.py`及原独立核验 |
| Q4归因与预测 | 最小二乘设计矩阵、经验残差积分、零总变化份额、分层支撑区间、分位线性规划、覆盖率、95%区间与90%前沿区别、101×101经验积分 | `Q4/evolution_model.py:ols/decompose/qreg/broad_distribution_predict` |

必须区分的验证口径：问题一仅在A4/A5的1M训练配方内做嵌套五折；问题二配比部分才把相同配方向量跨尺度分组。同一配方分组、整尺度留出、来源内留一和机构留出不是同一种验证。

公式排版采用LaTeX，完成渲染语法及控制字符检查。四问重新执行、镜像一致性与修改前161份CSV表的逐文件校验结果记录在 `formula_verification.json`。数值一致只说明说明补全未改变本次结果，不证明建模假设或因果解释成立。
