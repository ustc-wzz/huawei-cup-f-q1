# 项目记忆与协作约定

## 使用方式

本文件是本目录新对话的精简记忆。只保留用户已确认且当前文件仍采用的决定；当前用户明确要求优先。核对日期：2026-09-26；记忆修订 v0.9.3（仅文档），最新计算与ZIP仍为 v0.9.2：问题一采用平方相关冗余度，四问结果同步重算；问题二至四的模型结构沿用原方案。

- 问题一主文档是 `0924_F题_问题一.ipynb`，镜像为根目录 `notebook_source.py`；问题二主文档是 `0924_问题二.ipynb`，镜像为 `Q2/notebook_source.py`。不要根据“最新版”等旧文件名判断主版本。
- 需要公式、数据关系、验证边界时，按需读取 `PROJECT_MEMORY.md` 对应节；普通答疑无需重读六个窗口、所有 notebook 或所有附件。涉及具体结果时核对当前代码和结果文件。
- 用户要求忽略旧记忆、独立审查指定文件时，按指定范围判断。记忆帮助定位，不能替代证据。

## 问题一：质量评价与配比

- 问题一质量评价：22 个指标直接处理，不设四维中间分组；方向统一 → 独特词长度校正（20组）和两项格式指标领域校准 → A1参考正态映射 → 平方相关去冗余赋权（d_j=1+Σ_{k≠j}r_jk²，w_j∝d_j^−1；已替换|r_jk|） → 指数凹聚合，β=0.25。专业性保留原含义，A2/A3沿用A1参考。Q可为负，不是0—1概率。
- 冲突诊断：Pearson r≤−0.30，A1的20%/80%尾部分位，CI>0标记样本，候选对为空或权重和为零时CI=0；阈值是诊断口径，单独改变它不改变Q。降低冲突率不是优化目标，领域校准不能证明误判已消除。
- 领域配比与损失建模：比较线性混料、对数线性混料、加性样条和GBM。经综合权衡，选择对数线性模型作为主模型，用于关系解释与约束配比求解；GBM作为对照模型，其预测表现整体较强，用于比较和辅助核查。模型选择不等于单项预测指标排名第一。
- 质量评分与配比拟合是两条独立计算链：当前配比模型用17域比例预测13个验证域平均损失，不使用Q；更改β不改变其拟合和选模。7个质量域不等于17个配方域。后续连接Q时必须重新核对尺度与映射。
- 问题一冗余度独立对照（v0.7.2）：固定预处理与β=0.25，平方相关在44个副本实验均减小排名扰动；200次分层重抽样改善较小且非所有指标占优。42对盲评待人工标注，不能认定质量准确性提高；v0.8.0已将平方相关用于主模型并重算后续接口；盲评状态仍为待完成。入口 `output_q1_redundancy/实验报告.md`，脚本 `Q1_redundancy/`。

## 问题二：共同修正标度律

- 估计分阶段：B1对数残差Huber拟合；质量和配比等阶段用各自约束最小二乘，不可统称全部Huber拟合。
- 问题二已实现共同修正标度律 `L_s=E_s+(A_sN^-α+B_sD^-ν)exp(-λu+ηh)`，同时修正参数项和数据项，来源基线分别校准；u=Q−Q0(p)，h承接问题一对数线性主模型。N、D以十亿计，算力为6×10^18ND FLOPs。质量仅识别θ_Q=λs_Q，接口中λ、s_Q仍为空；联合迁移是假设，完整留尺度误差较大。固定质量、配比和来源时，共同乘子不改变训练算力最优N/D分配。旧有效数据量公式及GBM接口不再是本问主方案；当前后续接口为 `output_q2_shared/q2_interface.json`。

## 回答和证据要求

- 先给核心判断，再按“输入 → 处理 → 输出 → 用途”讲清最关键的1—3点；默认解释思路，用户问代码时再展开。术语首次出现用“术语＝具体含义”，必要时给一个真实样本或小算例。
- 比较模型同时说清双方优势与适用边界；区分训练拟合、交叉验证、检验集排序和跨尺度绝对误差。模型内部解释不是因果证明，预测候选配比不是实测最优。
- 区分原始观测、数据集作者的模型评分、本项目计算结果、半合成/插值/估算数据。优先核对 `real_attachments/source_manifest.json`；同一轨迹的检查点不能当独立实验，半合成数据不能冒充独立实测验证。
- 不为得到预期排名或冲突率倒推参数；不自动恢复已替换的方法。候选集合、指标口径或数据版本变化时明确说明，不能把“对手被删除”说成模型性能提升。
- 实验完成或用户改变主方案后，同步更新本文件和 `PROJECT_MEMORY.md` 中受影响的短条目，替换过时结论；讨论中建议保留为待定，不写成事实。不要追加逐轮聊天记录。

## 问题三：条件资源优化

- 问题三已确认采用方案一，舍弃方案二；七项选择均按推荐：p0主/p*对照；A1七域等权1%—99%线性成本映射；域内最高80%/50%/20%/10%/5%均值增量形成并列上限情景，50%仅为展示切片；11缺失域零新增收益且冻结Q_bar；s_Q=1主、0.5/2敏感性；允许显式规模外推；沿用题目同一D成本代理。执行规范见 `Q3/第三问notebook执行Prompt.md`，数据核查见 `Q3/quality_audit/核查与待决策.md`。第三问主文件 `0925_问题三.ipynb`，镜像 `Q3/notebook_source.py`，求解器 `Q3/resource_model.py`，结果 `output_q3_resource/`，已从头运行验证；当前结果承接平方相关重算后的一、二问接口；完整方法见`Q3/模型建立.md`。

- 全局质量收益阈值λ_on/λ_full（H_C割线极值，不能用局部导数替代），225个上限配置与1125个阈值核验通过；s_Q=1五档上限均R2。图07—12解释筛选、相位、敏感性、资源权衡及等效算力。新增模块 `Q3/decision_analysis.py`，入口 `output_q3_resource/decision_summary.md`；未识别真实λ或Token供给。

- 问题三50%参考切片结果：主情景在10^19—10^24 FLOPs、五个C7窗口和三种成本下均达到样本参照质量上限，未检测到质量状态转移；s_Q=2的幂函数成本在2048/4096/8192窗口各有R0→R1、R1→R2两处转移。不能把主情景无转移改写成三阶段；转移表为 `output_q3_resource/tables/transitions.csv`。质量正向外推、s_Q未识别及同一D原料供给假设仍保留。

## 问题四当前实现

- 第四问Markdown块公式使用`$$`分隔符，禁止混入退格等控制字符；v0.6.1已修复`\beta`损坏和块公式未渲染，并加入生成后的检查。

- 问题四主文档 `0925_问题四.ipynb`，镜像 `Q4/notebook_source.py`，结果 `output_q4_evolution/`。用户确认在可比条件下将剩余时间效应解释为非规模进步；全部计算观测仅来自自己的附件。主模型为时间→lnN/lnD→标度响应→能力的带噪声结构中介；完整样本仅35个基础模型、10家机构，不能把其归因推广为全部榜单的实证因果份额。
- 当前历史归因：2023-04-03→2024-06-07对比，规模56.98%、非规模43.02%，技术贡献95%区间包含零；Q3耦合基础模型前沿为条件外推，高可比Loss桥接对目标Loss不支持。大样本N-only预测仅为描述性，不能将其时间项再次称为纯非规模进步。预测起点2025-03-13，目标2026-03-13/2027-03-13；非运行日起算。
- C8原始JSON不删除或强行补全：4个损坏文件，1个目录有可解析替代，3个目录退出逐任务分析；台账和哈希保留。MuSR三叶子任务归一化后等权聚合。分类型短期回测中只有对话／微调模型优于上月延续；基础和合并模型未胜出，且结构前沿的分位覆盖不足，必须保留局限。

## 项目版本管理约定

用户要求：每次修改版本都同步到 GitHub，并说明版本如何区分。

- 固定主文档文件名，用清晰提交说明、语义版本标签和 CHANGELOG.md 区分每版。
- notebook修改后实际运行并验证，更新notebook_source.py文本镜像及关键结果，再提交和创建带说明的标签。
- 推送当前分支及新标签到已配置origin；禁止强推覆盖历史。
- 缺少远程地址或权限时保留本地提交，明确报告尚未上传，不声称同步完成。
- 原始数据、缓存及无关文件不纳入提交；遵循.gitignore。

## 复现包自动准备（v0.9.2）

最新本地交付为`F题_四问完整复现包_v0.9.2.zip`，旧包保持原样。入口`run_all.py`允许Python3.8+启动；非64位3.12/3.13通过`python_bootstrap.py`自动准备包内3.12运行环境，依赖安装到`.repro-env`。uv从PyPI下载并校验固定SHA256，Python位于`.repro-python`，不修改系统安装或PATH；`repro_runtime.py`定位并校验原始附件、注册随包中文字体，`signal_cache.py`校验或从完整原始数据重建缓存。四问显式UTF-8，Windows使用`run_windows.bat`；重复运行复用环境和完整缓存，更新当前输出。原始数据与独立环境不上传GitHub。模型沿用v0.8.0；Windows兼容性静态检查不能写成Windows实机运行验证。

## 模型说明完整性（v0.9.2）

正式方法来源为`Q1/模型建立.md`、`Q2/模型建立.md`、`Q3/模型建立.md`和`Q4/模型建立.md`。`refresh_reports.py`按来源刷新合并正文，Q3/Q4运行时刷新方法产物；修改公式须同步对应notebook说明与源镜像。保留指标构造、三类参照、边界情形、估计目标、约束求解及不确定性定义，不能以突出核心公式为由省去必要符号。问题一为1M训练集内嵌套五折，问题二配比迁移诊断才按完整配方跨尺度分组；两者不可混写。计算模型和参数不因本次说明补全而改变。

记忆更新只修改协作说明，不触发四问重算或重新打包；代码、公式、数据或结果实际改变时再按对应依赖链重跑。完整ZIP包含原始附件，Git仓库不含；两种交付方式不要混写为都需要手工补缓存。当前四问核验记录见`formula_verification.json`，不把旧版本号当作当前模型版本。

- v0.9.8独立论文流程图采用浅蓝、浅绿、浅橙三类圆角节点，文字仅置于节点内：`figures/q4_causal_publication.png`（600 dpi）及PDF/SVG；复现入口`Q4/draw_causal_publication.py`。显式展示Z节点及T→Z路径；本次未替换既有Notebook图或重建ZIP，数值模型不变。

- v0.9.9论文副本 `F题论文初稿_问题四重写_20260926.docx` 仅重写第四问，按历史归因→桥接检验→条件前沿→分类型对照组织，补入实际结果与验证边界。复现脚本 `rewrite_q4_paper.py`，正文快照及核验记录位于Q4；原稿与前三问保留，数值模型、Notebook及ZIP不变。

- v0.9.11第二问论文副本为 `F题论文初稿_问题二图文优化_20260926.docx`，在保留的v0.9.10精简副本上加入三张正文图：修正有效性→质量参数替代→配比最终损失响应；规模验证和完整17×17热图移入附录。脚本 `Q2/draw_core_figures.py`、`Q2/insert_core_figures.py`，图件、源数据及核验在 `output_q2_paper/`；公式与其他章节保留，未合入v0.9.9第四问副本；参数冻结，Notebook及复现ZIP不变。

- v0.9.13第三问论文副本 `F题论文初稿_问题三精简_20260926.docx` 基于用户原稿仅重写第三问：预算模型→降维求解→主情景配置与全局阈值→资源收益；正文5页、13个核心公式，辅助推导与设置移至6页附录三。脚本 `rewrite_q3_paper.py`，快照与核验位于Q3；保留主情景无转移及条件边界，未合入其他问的独立副本，数值模型、Notebook及ZIP不变。

- v0.9.14论文绘图：新增`绘图规范.md`；figures中图1—16删除顶部编号总标题，图1七域图例移到右下空白，图5删除左图并明确Pearson横轴，图6删除中间指标对子图并改为(a)/(b)两栏。第一问主Notebook及镜像同步，从头执行23个代码单元；20组PNG/SVG/PDF已导出，89个结果表经数值等价核验后保留原字节与接口哈希。模型、其他问及复现ZIP不变。核验记录在`figure_audit/`。

- v0.9.15绘图续改：图07删除右侧β敏感性折线子图，保留评分散点与调整量分布两栏；第一问图件通过`figure_typography.py`将Q_lin、Q_res、Q_i、p_i、a_i、g_i(p)、ρ、R²及加权质量公式改为LaTeX语法的数学排版，仅修改显示文字，数据键名不变。PNG/SVG/PDF与主Notebook同步更新；敏感性计算保留，模型和复现ZIP不变。

- v0.9.16第一问论文副本为`F题论文初稿_问题一5.6至5.10精简_20260926.docx`，基于用户原稿将范围扩展为5.6—5.10，按主模型→模型比较→质量增益→候选配比→验证组织；正文字符数由5588减至3207（减少42.6%），保留9个核心公式，13个补充公式、3幅原图及两张复现表移入附录，并补充完整17×13系数表和已有方向效应热图。脚本`Q1/restructure_paper_56_510.py`，快照、核验和修订说明位于`Q1/`；其他章节、原稿、原图数据与可编辑公式保留，模型、Notebook与复现ZIP不变。v0.9.12限定5.8—5.10的旧副本另存保留。

- v0.9.17第二问新增独立模型证据图：同划分四种修正位置比较→系数剖面目标→规模等损失线，入口`Q2/draw_model_evidence.py`，输出`output_q2_model_evidence/`。共同修正质量OOF RMSE为0.062225，最佳单项为0.073732；配比为0.094679，与仅数据项0.095787接近，不能声称明显优越。分开等贡献与算力最优分配条件；剖面不表示置信区间，λ与s_Q仍未分别识别。新增事后诊断未替换主模型、Notebook、论文、接口或ZIP。

- v0.9.18第四问论文绘图：图37—43及独立流程图按`绘图规范.md`统一重绘，输出PNG/SVG/PDF；变量采用LaTeX数学排版，子图编号为(a)/(b)/(c)，预测曲线同时区分颜色、线型和标记，图42样本说明移至右上空白。图37复用独立流程图函数，明确Z节点、T→Z及噪声路径；图下注释边界见`figure_audit/问题四绘图说明.md`。第四问Notebook从头运行，38份结果表等价核验后保留原字节，数值模型、前三问图件与复现ZIP不变。

- v0.9.19第四问独立月度观测前沿：入口`Q4/月度能力前沿.ipynb`，镜像`Q4/monthly_frontier_notebook_source.py`，绘图`Q4/draw_monthly_frontier.py`，产物`output_q4_monthly_frontier/`。沿用2449模型许可筛选与提交月份，前沿成员为得分≥月度90%分位；3月仅截至13日。2024年6—8月均值29.54，2024年12月—2025年2月40.23，差10.68分；描述性构成变化不能解释为因果效应，未改原四问模型、Notebook或ZIP。

- v0.9.21基于用户指定的`初稿/F题论文_问题四.docx`重构第四问，交付`F题论文_问题四重构_独立章节_v0.9.21.docx`与完整副本；独立正文6页、附录3页，主线为历史归因→损失换算检验→资源约束前沿→验证，保留11个正文与8个附录可编辑公式。补充既有冻结算力对照，收紧来源字段留出、删任务敏感性对象及两类重抽样口径，24个月区间下限按原始精度修正为23.05。脚本`Q4/restructure_q4_v0921.py`，审查、源稿、图件快照及核验位于`Q4/paper_restructure_v0921/`；原稿、其他章节、数值模型、Notebook和ZIP不变。

- v0.9.22图12拆分：从原SVG直接提取左右矢量子图，分别输出`figures/fig_12_marginal_effects_forest_left.svg`和`figures/fig_12_marginal_effects_forest_right.svg`；保留原始坐标、图例、数学文字与统计内容，原图、数据、Notebook及模型不变。复现入口`Q1/split_fig12_svg.py`，结构核验见`figure_audit/fig12_split.json`，两图均已完成渲染检查。
