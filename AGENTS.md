# 项目记忆与协作约定

## 使用方式

本文件是本目录新对话的精简记忆。只保留用户已确认且当前文件仍采用的决定；当前用户明确要求优先。核对日期：2026-09-25；问题四 v0.6.1（显示修正，计算沿用v0.6.0），问题三沿用 v0.5.0，问题一计算方案沿用 v0.2.0，问题二计算方案沿用 v0.3.0。

- 问题一主文档是 `0924_F题_问题一.ipynb`，镜像为根目录 `notebook_source.py`；问题二主文档是 `0924_问题二.ipynb`，镜像为 `Q2/notebook_source.py`。不要根据“最新版”等旧文件名判断主版本。
- 需要公式、数据关系、验证边界时，按需读取 `PROJECT_MEMORY.md` 对应节；普通答疑无需重读六个窗口、所有 notebook 或所有附件。涉及具体结果时核对当前代码和结果文件。
- 用户要求忽略旧记忆、独立审查指定文件时，按指定范围判断。记忆帮助定位，不能替代证据。

## 已确认的模型主线

- 问题一质量评价：22 个指标直接处理，不设四维中间分组；方向统一 → 独特词长度校正（20组）和两项格式指标领域校准 → A1参考正态映射 → 相关性去冗余赋权 → 指数凹聚合，β=0.25。专业性保留原含义，A2/A3沿用A1参考。Q可为负，不是0—1概率。
- 冲突诊断：Pearson r≤−0.30，A1的20%/80%尾部分位，CI>0标记样本；阈值是诊断口径，单独改变它不改变Q。降低冲突率不是优化目标，领域校准不能证明误判已消除。
- 领域配比与损失建模：比较线性混料、对数线性混料、加性样条和GBM。经综合权衡，选择对数线性模型作为主模型，用于关系解释与约束配比求解；GBM作为对照模型，其预测表现整体较强，用于比较和辅助核查。模型选择不等于单项预测指标排名第一。
- 质量评分与配比拟合是两条独立计算链：当前配比模型用17域比例预测13个验证域平均损失，不使用Q；更改β不改变其拟合和选模。7个质量域不等于17个配方域。后续连接Q时必须重新核对尺度与映射。
- 问题二已实现共同修正标度律 `L_s=E_s+(A_sN^-α+B_sD^-ν)exp(-λu+ηh)`，同时修正参数项和数据项，来源基线分别校准；u=Q−Q0(p)，h承接问题一对数线性主模型。N、D以十亿计，算力为6×10^18ND FLOPs。质量仅识别θ_Q=λs_Q，接口中λ、s_Q仍为空；联合迁移是假设，完整留尺度误差较大。固定质量、配比和来源时，共同乘子不改变训练算力最优N/D分配。旧有效数据量公式及GBM接口不再是本问主方案；当前后续接口为 `output_q2_shared/q2_interface.json`。

## 回答和证据要求

- 问题三已确认采用方案一，舍弃方案二；七项选择均按推荐：p0主/p*对照；A1七域等权1%—99%线性成本映射；域内最高80%/50%/20%/10%/5%均值增量形成并列上限情景，50%仅为展示切片；11缺失域零新增收益且冻结Q_bar；s_Q=1主、0.5/2敏感性；允许显式规模外推；沿用题目同一D成本代理。执行规范见 `Q3/第三问notebook执行Prompt.md`，数据核查见 `Q3/quality_audit/核查与待决策.md`。第三问主文件 `0925_问题三.ipynb`，镜像 `Q3/notebook_source.py`，求解器 `Q3/resource_model.py`，结果 `output_q3_resource/`，已从头运行验证；一、二问接口不变。

- 先给核心判断，再按“输入 → 处理 → 输出 → 用途”讲清最关键的1—3点；默认解释思路，用户问代码时再展开。术语首次出现用“术语＝具体含义”，必要时给一个真实样本或小算例。
- 比较模型同时说清双方优势与适用边界；区分训练拟合、交叉验证、检验集排序和跨尺度绝对误差。模型内部解释不是因果证明，预测候选配比不是实测最优。
- 区分原始观测、数据集作者的模型评分、本项目计算结果、半合成/插值/估算数据。优先核对 `real_attachments/source_manifest.json`；同一轨迹的检查点不能当独立实验，半合成数据不能冒充独立实测验证。
- 不为得到预期排名或冲突率倒推参数；不自动恢复已替换的方法。候选集合、指标口径或数据版本变化时明确说明，不能把“对手被删除”说成模型性能提升。
- 实验完成或用户改变主方案后，同步更新本文件和 `PROJECT_MEMORY.md` 中受影响的短条目，替换过时结论；讨论中建议保留为待定，不写成事实。不要追加逐轮聊天记录。

- 问题三v0.5.0新增全局质量收益阈值λ_on/λ_full（H_C割线极值，不能用局部导数替代），225个上限配置与1125个阈值核验通过；s_Q=1五档上限均R2。图07—12解释筛选、相位、敏感性、资源权衡及等效算力。新增模块 `Q3/decision_analysis.py`，入口 `output_q3_resource/decision_summary.md`；未识别真实λ或Token供给。

- 问题三50%参考切片结果：主情景在10^19—10^24 FLOPs、五个C7窗口和三种成本下均达到样本参照质量上限，未检测到质量状态转移；s_Q=2的幂函数成本在2048/4096/8192窗口各有R0→R1、R1→R2两处转移。不能把主情景无转移改写成三阶段；转移表为 `output_q3_resource/tables/transitions.csv`。质量正向外推、s_Q未识别及同一D原料供给假设仍保留。

## 问题四当前实现

- 第四问Markdown块公式使用`$$`分隔符，禁止混入退格等控制字符；v0.6.1已修复`\beta`损坏和块公式未渲染，并加入生成后的检查。

- 问题四主文档 `0925_问题四.ipynb`，镜像 `Q4/notebook_source.py`，结果 `output_q4_evolution/`。用户确认在可比条件下将剩余时间效应解释为非规模进步；全部计算观测仅来自自己的附件。主模型为时间→lnN/lnD→标度响应→能力的带噪声结构中介；完整样本仅35个基础模型、10家机构，不能把其归因推广为全部榜单的实证因果份额。
- 问题四 v0.6.0：2023-04-03→2024-06-07对比，规模56.98%、非规模43.02%，技术贡献95%区间包含零；Q3耦合基础模型前沿为条件外推，高可比Loss桥接对目标Loss不支持。大样本N-only预测仅为描述性，不能将其时间项再次称为纯非规模进步。预测起点2025-03-13，目标2026-03-13/2027-03-13；非运行日起算。
- C8原始JSON不删除或强行补全：4个损坏文件，1个目录有可解析替代，3个目录退出逐任务分析；台账和哈希保留。MuSR三叶子任务归一化后等权聚合。分类型短期回测中只有对话／微调模型优于上月延续；基础和合并模型未胜出，且结构前沿的分位覆盖不足，必须保留局限。

## 项目版本管理约定

用户要求：每次修改版本都同步到 GitHub，并说明版本如何区分。

- 固定主文档文件名，用清晰提交说明、语义版本标签和 CHANGELOG.md 区分每版。
- notebook修改后实际运行并验证，更新notebook_source.py文本镜像及关键结果，再提交和创建带说明的标签。
- 推送当前分支及新标签到已配置origin；禁止强推覆盖历史。
- 缺少远程地址或权限时保留本地提交，明确报告尚未上传，不声称同步完成。
- 原始数据、缓存及无关文件不纳入提交；遵循.gitignore。

- 问题一冗余度独立对照（v0.7.2）：固定预处理与β=0.25，平方相关在44个副本实验均减小排名扰动；200次分层重抽样改善较小且非所有指标占优。42对盲评待人工标注，不能认定质量准确性提高；原绝对相关主模型与后续接口不变。入口 `output_q1_redundancy/实验报告.md`，脚本 `Q1_redundancy/`。
